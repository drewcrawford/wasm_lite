[package]
name = "wasm_lite_macro"
version = "0.1.0"
edition = "2024"
description = "Procedural macros for wasm_lite."

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
